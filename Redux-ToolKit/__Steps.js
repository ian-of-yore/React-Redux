/**
 * 01.import createSlice from redux-toolkit
 * 02.create a featureSlice with the imported createSlice with properties such as name, initialStatea and reducer
 * 03.export the reducer and the actions
 * 04.Import the reducer in store.js and attach it to the store
 * 05.Import the actions in index.js and dispatch the actions
 */